# cp .env.example .env && docker compose up -d --build  →  http://<host>:7860
services:
  meridian:
    build: .
    image: meridian-mcp:latest
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      PORT: "7860"
    restart: unless-stopped
